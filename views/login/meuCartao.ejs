<!DOCTYPE html>
<html lang="pt-BR">
  <meta charset="UTF-8">
  <title>Meu Cartão - Cliente</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap 4.1.3 -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styleCartao.css">
</head>
<body>
  <div class="card">
  <h1>Cartão Fidelidade Digital</h1>
<div class="card-header">
    <h2>Ao realizar 10 refeições, ganhe um almoço grátis.</h2>
  </div>

  <!-- Quadrado grande -->
  <div class="big-square">
    <div class="punch-grid" id="punch-grid">
    <% for (let i = 0; i < 10; i++) { %>
        <div class="punch-box <%= i < refeicoes.length ? '' : 'blank' %>">
          <%= i < refeicoes.length ? '✅' : '⬜' %>
        </div>
      <% } %>
  </div>
</div>

  <!-- Botão condicional -->
  <div class="text-center mt-4">
    <% if (refeicoes.length >= 10) { %>
      <!-- Usuário completou o cartão -->
      <a href="/premio?user=<%= username %>" class="btn btn-success btn-lg">
        🎁 Resgatar Meu Prêmio
      </a>
    <% } else { %>
      <!-- Usuário ainda não completou -->
      <button class="btn btn-secondary btn-lg" disabled>
        ⏳ Complete <%= 10 - refeicoes.length %> refeições para ganhar
      </button>
    <% } %>
  </div>
</body>
</html>
