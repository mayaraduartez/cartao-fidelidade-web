<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Promoção do Dia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style5.css" />
  
  

</head>
<body>

<!-- ⭐ NAVBAR IGUAL À DA HOME -->
<nav class="navbar navbar-expand-lg navbar-custom fixed-top">
  <div class="container">
    <a class="navbar-brand" href="/home">
      <img src="/images/logoRes.png" alt="Logo do Sistema">
    </a>

    <span class="nav-text">Promoção do Dia</span>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSite">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSite">

      <ul class="navbar-nav">
        <li class="nav-item mr-3">
          <span class="nav-text"><%= username %></span>
        </li>

        <li class="nav-item">
          <a href="/logout" class="btn btn-logout">Sair</a>
        </li>
      </ul>

    </div>
  </div>
</nav>


<!-- ⭐ CONTEÚDO -->
<div class="container">

  <h1 class="titulo-promocoes text-center">🌟 Promoção do Dia 🌟</h1>

  <% if (promocoes && promocoes.length > 0) { %>
    <div class="row">
      <% promocoes.forEach(promo => { %>
        <div class="col-md-6 mb-4">
          <div class="card">

            <% if (promo.foto) { %>
              <img src="/uploads/<%= promo.foto %>" class="card-img-top" style="height:200px; object-fit:cover;">
            <% } %>

            <div class="card-body">
              <h5 class="card-title"><%= promo.nome %></h5>
              <p><%= promo.descricao %></p>
              <p><strong>Quantidade:</strong> <%= promo.qtd_refeicao %></p>
              <p><strong>Desconto:</strong> <%= promo.tipo_desconto %></p>
              <p><strong>Valor:</strong> R$ <%= promo.valor %></p>

              <p><strong>Válido de:</strong>
                <%= new Date(promo.data_inicio).toLocaleDateString('pt-BR') %> até
                <%= promo.data_fim ? new Date(promo.data_fim).toLocaleDateString('pt-BR') : '—' %>
              </p>
            </div>

          </div>
        </div>
      <% }) %>
    </div>

  <% } else { %>

    <div class="alert alert-info text-center">
      Nenhuma promoção disponível no momento.
    </div>

  <% } %>

  <div class="text-center mt-4">
    <a href="/home" class="btn-voltar">Voltar</a>
  </div>

</div>

</body>
</html>
