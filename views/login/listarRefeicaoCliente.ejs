<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tela Refeições do Cliente</title>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style3.css">
</head>

<body>
  <div class="tela-funcionarios">
    <!-- Cabeçalho -->
    <div class="cabecalho-funcionarios">
      <div class="logo-area">
        <img src="/images/logoRes.png" alt="Logo" />
      </div>
      <h1 class="titulo-funcionarios">Conceder Prêmio ao Cliente</h1>
    </div>


    <!-- Caixa da tabela -->
    <div class="box-tabela mt-3">
      <h2>Dados do Cliente</h2>

      <table class="table">
        <thead>
          <tr>
            <th>Nome</th>
            <th>CPF</th>
            <th>Email</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= cliente.nome %></td>
            <td><%= cliente.cpf %></td>
            <td><%= cliente.email %></td>
          </tr>
        </tbody>
      </table>
    </div>

    <!-- Botões / mensagem: agora abaixo da tabela -->
    <div class="mt-4">

      <% if (premio) { %>
        <form action="/utilizar-premio/<%= premio.id %>?user=<%= username %>" method="POST">
          <button type="submit" class="btn btn-success btn-lg">Utilizar Prêmio</button>
        </form>
      <% } else { %>
        <p class="msg-erro-custom">Nenhum prêmio disponível para este cliente.</p>
      <% } %>

      <!-- Botão voltar -->
      <div class="mt-3">
        <a href="/admin/refeicoes/novo" class="btn btn-primary">Voltar para Comanda</a>
      </div>

    </div>

  </div>

</body>
</html>
